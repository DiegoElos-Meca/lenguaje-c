/* TODO (#1#): El operador % en lenguaje C, calcula el residuo 
               que queda al dividir dos números enteros; el 
               siguiente programa muestra lo que realiza ese 
               operador: 
                */

#include <stdio.h> 
#include <conio.h> 
main(  ) {  
int x=-7,y=2  ;  

printf("%d",x%y);  //imprime el residuo de dividir 'x' y 'y'
getch(); 
return 0;
}
